# API URL (в продакшне — URL твоего Railway/Render сервера)
VITE_API_URL=http://localhost:4000/api

# Firebase (скопируй из Firebase Console → Project Settings)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# PostgreSQL
# Локально: оставь пустым (используется localhost/lexis_db)
# На Railway: вставь DATABASE_URL из настроек базы
DATABASE_URL=

# CORS: через запятую домены фронтенда (нужно на Railway/Render)
# ALLOWED_ORIGINS=https://твой-сайт.vercel.app

# Порт сервера (Railway сам задаёт PORT)
# PORT=4000
