# 🚀 Как запустить Lexis

## Быстрый старт

### 1. Запусти PostgreSQL сервер (в отдельном терминале)
```bash
cd /Users/leontrofimenko/Desktop/EnglishStudy
PORT=4000 node server.js
```

### 2. Запусти фронтенд (в другом терминале)
```bash
cd /Users/leontrofimenko/Desktop/EnglishStudy
npm run dev
```

### 3. Открой в браузере
```
http://localhost:3000
```

## Что работает

✅ **PostgreSQL база данных** (вместо Firestore)
✅ **Firebase Auth** (Google OAuth)
✅ **API сервер** на Express.js (порт 4000)
✅ **Все функции**:
  - Регистрация/авторизация
  - Выбор роли (teacher/student)
  - Выбор родного языка
  - Изучение слов (learn)
  - Прогресс сохраняется
  - Просмотр статистики (profile)
  - Списки слов (word-list)

## Проверка что все работает

```bash
# Проверь API сервер
curl http://localhost:4000/health

# Должен вернуть:
{"status":"ok","database":"postgresql"}
```

## Если что-то не работает

### PostgreSQL не запущен?
```bash
brew services start postgresql@14
```

### API сервер не отвечает?
```bash
# Убей старые процессы
pkill -f "node server.js"

# Запусти снова
PORT=4000 node server.js
```

### База данных не создана?
```bash
psql postgres -c "CREATE DATABASE lexis_db;"
```

## API Endpoints

- `POST /api/users` - Создать/получить пользователя
- `GET /api/users/:uid` - Получить пользователя
- `PATCH /api/users/:uid` - Обновить пользователя
- `GET /api/progress/:uid/:tier` - Получить прогресс
- `POST /api/progress/:uid/:tier` - Сохранить прогресс
- `POST /api/progress/:uid/:tier/batch` - Сохранить массово
- `GET /api/progress/:uid/:tier/stats` - Статистика
